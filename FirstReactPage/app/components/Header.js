'use strict';

var React = require('react');

var Header = React.createClass({
	render: function() {
		return(
			<header>
			<h1>Cleo First React Page</h1>
			</header>
		);
		
	}
});

module.exports = Header;